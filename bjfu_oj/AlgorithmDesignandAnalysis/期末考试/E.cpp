#include <bits/stdc++.h>
using namespace std;

int main()
{
	int n, m;
	string str;
	cin >> n >> m;

	vector<string> maze;

	for (int i = 0; i < n; ++i)
	{
		cin >> str;
		maze.push_back(str);
	}

	// B 开始
	// E 结束
	// T 可以走
	// F 不能走
	return 0;
}
