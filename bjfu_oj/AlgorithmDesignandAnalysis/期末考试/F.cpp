#include <bits/stdc++.h>
using namespace std;

int main()
{
	int n, tmp;

	cin >> n;

	vector<int> T, A;

	for (int i = 0; i < n; ++i)
	{
		cin >> tmp;
		T.push_back(tmp);
	}

	for (int i = 0; i < n; ++i)
	{
		cin >> tmp;
		A.push_back(tmp);
	}

	return 0;
}
